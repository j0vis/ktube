# ktube translations

`ktube.pot` generated from PHP sources via WP-CLI:

```sh
wp i18n make-pot . languages/ktube.pot
```

Commit generated POT alongside each phase's strings. Don't commit `.po`/`.mo` for languages ktube's maintainers don't actively translate.
